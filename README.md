1. Update `.sentryclirc` with your project name, organization name and API token that can be obtained here https://sentry.io/api/
2. Update `foo.ts` with your `DSN` and `RELEASE`
3. `npm build`
4. `RELEASE=sameAsInFoo npm run upload`
